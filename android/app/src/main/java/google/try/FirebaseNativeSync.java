package google.try;

import android.util.Log;

public class FirebaseNativeSync {
    public static void updateLocation(double latitude, double longitude) {
        // Placeholder for Firebase Sync
        Log.d("FirebaseNativeSync", "Syncing Location: " + latitude + ", " + longitude);
    }
}
